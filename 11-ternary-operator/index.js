const userName = "Zeynepnursaltikk"
const password = "Zeynepnursaltikk123"

password.length > 18 ? console.log("Çalıştı") : console.log("18'den büyük olmadığı için çaşılmadı")
userName.length > 16 ? console.log("username doğru") : console.log("doğru değil")

if (password.length > 20) {
    console.log("Çalıştı")
} else {
    console.log("20'den büyük olmadığı için çalışmadı")
}